- name: Install vim
  hosts: localhost
  become: true
  
  tasks:

  - name: Add some repo
    yum:
      name: epel-release
      state: present
      tags:
      - vim

  - name: Install vim
    yum:
      name: vim
      state: present
      tags:
      - vim
